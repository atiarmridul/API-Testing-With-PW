#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

bash scripts/update-agent-progress.sh
bash scripts/sync-markdown.sh

# Ensure auto-regenerated markdown updates are included in the commit automatically.
git add -A -- '*.md' ':(glob)**/*.md'
