#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

bash scripts/sync-markdown.sh

# Ensure regenerated markdown index is included in the commit automatically.
if [ -f docs/index.md ]; then
  git add docs/index.md
fi
